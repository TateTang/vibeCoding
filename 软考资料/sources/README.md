# 题源模板说明

本目录放 2019-2024 年中级软件设计师上午真题题源模板。

每个 JSON 文件对应一套上午卷。填题时保持如下规则：

- `number`：原卷题号。
- `stem`：题干。
- `options`：原始选项，支持 `A`、`B`、`C`、`D`。
- `answer`：原始答案，单选写字符串，例如 `"C"`；多选可写数组，例如 `["A", "C"]`。
- `analysis`：解析，可不填。

运行 `node scripts/generate-papers.mjs` 后，脚本会在 `output` 目录生成打乱版试题和答案。